services:
  redis:
    image: redis:alpine
    container_name: sentient-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --requirepass test
    env_file:
      - ./src/server/.env
    volumes:
      - redis_data:/data

volumes:
  redis_data:
